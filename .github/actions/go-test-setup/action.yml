name: installipfs
description: install go-ipfs

runs:
  using: "composite"
  steps:
    - name: Step 1
      shell: bash
      run: (cd /tmp && go install github.com/ipfs/go-ipfs/cmd/ipfs@master)
